{
  "name": "the-gardenia",
  "version": "1.0.0",
  "description": "The Gardenia - Luxury Apartments",
  "private": true,
  "scripts": {
    "build": "if [ -f frontend/package.json ]; then npm install --prefix frontend && npm run --prefix frontend build; else npm install && npm run build; fi"
  }
}
