{
  "installCommand": "npm install --workspaces --include-workspace-root --legacy-peer-deps --no-audit --no-fund",
  "buildCommand": "npm run build --workspace frontend",
  "outputDirectory": "frontend/build"
}
